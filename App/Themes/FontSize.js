import { totalSize } from 'react-native-dimension';

const FontSize = {
  h1: totalSize(3.75),
  h2: totalSize(2.75),
  h3: totalSize(2),
  h4: totalSize(1.75),
  large: totalSize(1.75),
  medium: totalSize(1.5),
  small: totalSize(1.25),
  tiny: totalSize(1),
}


export default FontSize;
